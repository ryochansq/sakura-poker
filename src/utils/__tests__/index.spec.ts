// import { getCombos } from '@App/utils/score'
// import { members, Member } from '@App/interfaces/index'
export { }

describe('calc score distribution', () => {
  it('score distribution', () => {
    // const n = members.length
    // let arr = []
    // for (let i = 0; i <= 63; i++) arr[i] = 0
    // for (let i = 0; i < n - 4; i++) for (let j = i + 1; j < n - 3; j++)for (let k = j + 1; k < n - 2; k++)for (let l = k + 1; l < n - 1; l++) for (let m = l + 1; m < n; m++) {
    //   const nowMembers = [members[i], members[j], members[k], members[l], members[m]] as Member[]
    //   const combos = getCombos(nowMembers)
    //   const score = combos.reduce((acc, combo) => acc + combo.score, 0)
    //   arr[score / 5]++
    // }
    // let st = ''
    // for (let i = 0; i <= 63; i++) st += `${i * 5}:${arr[i]}\n`
    // console.info(st)
    expect(2).toStrictEqual(2)
  })
})